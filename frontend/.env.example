# Backend API Base URL
# This should point to your Cloudflare Worker API
VITE_API_BASE_URL=http://localhost:8787

# Note: The backend also needs FRONTEND_URL configured in wrangler.toml
# to redirect OAuth callbacks to the correct frontend URL
